#include <stdio.h>
int main()
{
int n, i,a,b;
printf("Enter an integer: ");
scanf("%d",&n);
for(i=1; i<=10; ++i)
{
a=n*i;  
printf(" %d \n",a);
if(a%2==0&&2n-1==3)
printf("pair up the childrens");
}
return 0;
}
